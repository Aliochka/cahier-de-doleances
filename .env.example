# Example environment configuration
# Default to the lightweight dev/test database
GDN_DB_PATH=./sandbox.db
# Optional: override alembic database URL directly (takes precedence if set)
# DATABASE_URL=sqlite:///./sandbox.db
ENV="dev"
SITEMAP_CHUNK=200
SITEMAP_MAX_URLS=50000
SITEMAP_LIGHT_LASTMOD=1
CANONICAL_HOST_URL=https://www.cahierdedoleances.fr